-- =============================================================================
-- ผลกระทบในพื้นที่ที่แจ้งมากับรายงานจุดน้ำท่วม (ถูกตัดไฟ / ตัดน้ำประปา / ขาดอาหาร / ขาดน้ำดื่ม / ต้องการที่พัก)
-- เก็บเป็น code คั่นด้วย comma ตาม flood_area_impacts() ใน libs/helpers.php
--
-- ปกติไม่ต้องรันเอง: ระบบเพิ่มคอลัมน์ให้ตอนมีคนส่งรายงานครั้งแรก
-- ถ้าบัญชีฐานข้อมูลของเว็บไม่มีสิทธิ์ ALTER ให้รันไฟล์นี้เอง (รันซ้ำได้)
--   php sql/apply_schema.php 09
-- =============================================================================

ALTER TABLE `flood_report`
  ADD COLUMN `impacts` VARCHAR(100) DEFAULT NULL COMMENT 'คั่น comma: no_power,no_tap,food,water,shelter' AFTER `trend`;
