# SK Flood — ระบบช่วยเหลือผู้ประสบภัยน้ำท่วม จังหวัดสระแก้ว

PHP MVC แบบเดียวกับระบบ COC (`libs/` + `controllers/` + `models/` + `views/`)
ฐานข้อมูล MySQL/MariaDB `db_flood` บน **192.168.0.18** — ตารางขึ้นต้นด้วย `flood_`

## ติดตั้ง

1. โฟลเดอร์: `Z:\Flood2026` → เว็บ `http://<server>/Flood2026/`
   ต้องการ PHP 8 ที่เปิด `pdo_mysql`, `mbstring`, `fileinfo` และ `gd` (ย่อรูป/ลบพิกัดในรูป — ไม่มีก็ใช้ได้ แต่จะเก็บรูปเดิม)
2. คัดลอก `config/app.sample.php` เป็น `config/app.php` แล้วแก้ `DB_USER` / `DB_PASS`
   (`DB_HOST` = 192.168.0.18 และ `DB_NAME` = db_flood ตั้งไว้แล้ว)
3. เตรียมบัญชีฐานข้อมูล (ทำครั้งเดียวบน 192.168.0.18 — เปลี่ยน IP เป็นเครื่องที่รัน PHP):
   ```sql
   CREATE DATABASE IF NOT EXISTS db_flood CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   CREATE USER 'flood'@'<IP เครื่องเว็บ>' IDENTIFIED BY '<รหัสผ่าน>';
   GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, REFERENCES ON db_flood.* TO 'flood'@'<IP เครื่องเว็บ>';
   ```
4. สร้างตาราง (รันซ้ำได้ ไม่ลบข้อมูลเดิม):
   ```
   php sql/apply_schema.php
   ```
   หรือรันไฟล์ `sql/01` → `sql/10` ตามลำดับด้วย HeidiSQL / Navicat / phpMyAdmin ก็ได้
5. ให้ PHP เขียนโฟลเดอร์ `public/uploads/flood/` ได้ (เก็บรูปที่แนบมา)
6. เปิด `http://<server>/Flood2026/login` → **admin / admin** → ระบบบังคับให้ตั้งรหัสผ่านใหม่ทันที

### เว็บเซิร์ฟเวอร์
- **Apache / XAMPP** — ใช้ `.htaccess` ที่มากับระบบ (เปิด `mod_rewrite` และ `AllowOverride All`)
- **nginx** — nginx ไม่อ่าน `.htaccess` ต้องใส่ `deploy/nginx-flood-protect.conf` ใน `server { }`
  ไม่ใส่ = ไฟล์ `config/app.php`, `sql/`, รูปที่อัปโหลด เปิดตรงจากเว็บได้
- **HTTPS** — ปุ่ม "ใช้ตำแหน่งปัจจุบัน" (GPS) ในฟอร์มประชาชน เบราว์เซอร์ยอมให้ใช้เฉพาะเว็บ https
  ถ้ายังเป็น http ประชาชนยังใช้ได้โดยแตะบนแผนที่เพื่อปักหมุดเอง

## หน้าจอ

| หน้า | URL | ใครใช้ |
|------|-----|-------|
| แผนที่สถานการณ์น้ำ | `/Flood2026/` | ประชาชน (ไม่ต้องล็อกอิน) |
| แจ้งจุดน้ำท่วม | `/Flood2026/report` | ประชาชน |
| ขอความช่วยเหลือ | `/Flood2026/sos` | ประชาชน |
| ติดตามคำขอ | `/Flood2026/sos/status` | ประชาชน (ใช้เลขอ้างอิง + เบอร์โทร) |
| API พื้นที่ประกาศ (JSON) | `/Flood2026/api/zones` | สาธารณะ — ไม่มีข้อมูลส่วนบุคคล |
| ภาพรวม | `/Flood2026/flood` | เจ้าหน้าที่ |
| พื้นที่ประกาศ / วาดพื้นที่ | `/Flood2026/flood/zones` · `flood/zoneForm` | officer ขึ้นไป (viewer ดูได้) |
| รายงานจากประชาชน | `/Flood2026/flood/reports` | officer ขึ้นไป |
| คำขอความช่วยเหลือ / ใบงาน | `/Flood2026/flood/help` · `flood/helpView/<id>` | officer, team (เฉพาะงานของทีม) |
| รับเรื่องทางโทรศัพท์ | `/Flood2026/flood/helpForm` | officer ขึ้นไป |
| ทะเบียนกลุ่มเปราะบาง | `/Flood2026/flood/vulnerable` | officer ขึ้นไป |
| ทีมช่วยเหลือ | `/Flood2026/flood/teams` | officer ขึ้นไป |
| นำเข้าพื้นที่จาก arankub.com | `/Flood2026/flood/zoneImport` (ปุ่มบนหน้าพื้นที่ประกาศ) | admin |
| ผู้ใช้งาน | `/Flood2026/flood/settingsUsers` | admin |
| ผู้ใช้งานออนไลน์ | `/Flood2026/flood/onlineUsers` | admin |
| ประวัติการเข้าใช้งาน / แก้ไขข้อมูล | `flood/loginLog` · `flood/auditLog` | admin |

## สิทธิ์ผู้ใช้

| role | ทำอะไรได้ |
|------|-----------|
| `super_admin` | ทุกอย่าง รวมถึงจัดการบัญชีผู้ดูแลสูงสุด |
| `admin` | ทุกเมนู จัดการผู้ใช้ ทีม ดูประวัติการใช้งาน |
| `officer` | ประกาศพื้นที่ ตรวจรายงาน รับเรื่อง/มอบหมายใบงาน ดูแลทะเบียนกลุ่มเปราะบาง |
| `team` | เห็นเฉพาะใบงานของทีมตัวเอง — เริ่มงาน / ปิดงาน / บันทึก |
| `viewer` | ดูภาพรวมและพื้นที่ประกาศ ไม่เห็นข้อมูลรายบุคคล |

ผู้ดูแลเปลี่ยนสิทธิ์ / ปิดบัญชี / สั่งออกจากระบบ มีผลภายใน 1 นาทีโดยไม่ต้องรอผู้ใช้ล็อกอินใหม่

## ผู้ใช้งานออนไลน์
- ทุกหน้าของระบบเจ้าหน้าที่ส่งสัญญาณ (`flood/ping`) ทุก 60 วินาที → บันทึก `flood_user.last_seen_at` และหน้าที่เปิดอยู่ (`last_page`)
- "ออนไลน์" = ใช้งานภายใน `ONLINE_MINUTES` นาที (ค่าเริ่มต้น 3) — ปรับได้ใน `config/app.php`
- แถบด้านบนแสดงจำนวนคนออนไลน์ กดดูรายชื่อได้ (ทุกคนที่ล็อกอินเห็น)
- หน้า **ผู้ใช้งานออนไลน์** (admin) รีเฟรชทุก 15 วินาที เห็น IP / หน้าที่เปิด / เวลาเข้าสู่ระบบ และกด "ให้ออก" ได้

## ขั้นตอนงาน
- **รายงานประชาชน** → รอตรวจ → (โทรยืนยัน) → สร้างพื้นที่ประกาศใหม่จากรายงาน / ผูกกับพื้นที่เดิม / ไม่ใช้ข้อมูล
- **คำขอความช่วยเหลือ** → รับเรื่องใหม่ → โทรยืนยันแล้ว → มอบหมายทีม → กำลังช่วยเหลือ → ช่วยเหลือแล้ว (หรือยกเลิกพร้อมเหตุผล)
  ระบบประเมินความเร่งด่วนเบื้องต้น (ต้องอพยพ + มีกลุ่มเปราะบาง = ด่วนมาก) เจ้าหน้าที่ปรับได้
- **ทะเบียนกลุ่มเปราะบาง** → ปักหมุดบ้าน → ระบบเทียบกับพื้นที่ประกาศทุกครั้งที่เปิดหน้า แล้วเตือนคนที่อยู่ในพื้นที่แต่ยังไม่อพยพ
- **ระดับพื้นที่** (ผู้ดูแลระบบ → ระดับพื้นที่) — ตั้งต้น 6 ระดับ เรียงจากรุนแรงมากไปน้อย:
  อพยพแล้ว · รถทุกชนิดผ่านไม่ได้ · ถนนขาด / สะพานชำรุด · รถเล็กผ่านไม่ได้ · เฝ้าระวัง · น้ำลดแล้ว (ยังต้องระวัง)
  ผู้ดูแลเพิ่มระดับ เปลี่ยนชื่อ สี ไอคอน ลำดับ และปิดใช้งานได้เอง — สีบนแผนที่ คำอธิบายสี ตัวเลขสรุป และป้ายทุกหน้าเปลี่ยนตาม
  ระดับที่ยังมีพื้นที่ประกาศอยู่ปิดใช้ไม่ได้ · ระดับตั้งต้นลบไม่ได้ (ปิดใช้แทน) · ระดับที่ผู้ดูแลเพิ่มเองลบได้ถ้ายังไม่เคยมีพื้นที่ใช้
  ตาราง `flood_zone_level` ระบบสร้างเองตอนผู้ดูแลเปิดหน้านี้ครั้งแรก (ถ้าบัญชีฐานข้อมูลไม่มีสิทธิ์ CREATE ให้รัน `sql/08_flood_zone_level.sql`)
  ระหว่างที่ยังไม่มีตาราง ระบบใช้ 6 ระดับตั้งต้นจากโค้ด
- **ภาพประกอบพื้นที่** (แสดงในบอลลูนบนแผนที่สาธารณะ แตะเพื่อดูเต็มจอ) — เจ้าหน้าที่เพิ่มได้ 2 ทาง:
  ฟอร์มประกาศ/แก้ไขพื้นที่ (อัปโหลดภาพ หรือติ๊กรูปจากรายงานประชาชนที่ผูกกับพื้นที่) และกล่องตรวจรายงาน (ติ๊ก "นำรูปจากรายงานนี้ไปแสดง" ตอนผูกกับพื้นที่)
  รูปรายงานประชาชน **ไม่ขึ้นแผนที่เอง** ต้องมีเจ้าหน้าที่เลือกทุกครั้ง · สูงสุด 6 ภาพต่อพื้นที่ (`define('ZONE_PHOTO_MAX', 6);` ใน config) · ภาพล่าสุดเป็นภาพแรก
  ภาพถูกคัดลอกเป็นไฟล์ใหม่ที่เข้ารหัส JPEG ใหม่ (ไม่มี EXIF/พิกัดกล้อง) + ภาพย่อ 640px — **ต้องมี PHP GD** (`php-gd`) ไม่งั้นฟอร์มจะแจ้งว่าเผยแพร่ภาพไม่ได้
  ประชาชนเปิดภาพได้เฉพาะของพื้นที่ที่ประกาศอยู่ (`api/zoneThumb/<id>`, `api/zonePhoto/<id>`) ปิดประกาศแล้วเห็นเฉพาะเจ้าหน้าที่
- **นำเข้าพื้นที่จาก arankub.com** (ผู้ดูแล: พื้นที่ประกาศ → ปุ่ม "นำเข้าจาก arankub.com") — **คัดลอกครั้งเดียว** ไม่อัปเดตตามต้นทางเอง
  1. กด "ดึงข้อมูลจาก arankub.com" (เซิร์ฟเวอร์อ่าน `https://arankub.com/api/flood` ด้วย PHP curl)
     เซิร์ฟเวอร์ออกอินเทอร์เน็ตไม่ได้ → เปิดลิงก์นั้นในเบราว์เซอร์ Ctrl+A, Ctrl+C แล้ววางในช่อง "คัดลอกข้อมูลมาวางเอง" (หรือบันทึกเป็นไฟล์ .json แล้วเลือกไฟล์)
  2. ตรวจบนแผนที่/ตาราง ก่อนบันทึก: เทียบระดับ (BLOCKED "รถเข้าไม่ได้" → ตั้งต้นเป็น รถเล็กผ่านไม่ได้ เปลี่ยนได้) · ติ๊กเลือกรายการ
     ระบบไม่ติ๊กให้เองถ้าอาจซ้ำกับพื้นที่ที่ประกาศอยู่ หรืออยู่นอกจังหวัด · เตือนถ้าข้อความมีเบอร์โทร
  3. กด "นำเข้า" → เป็นพื้นที่ของระบบนี้ทันที (แหล่งที่มา "นำเข้าจาก arankub.com") ขึ้นแผนที่ประชาชน แก้ไข/ปิดประกาศได้ตามปกติ
  การแปลงรูปร่าง: วงกลม/ขอบเขตใช้ตามเดิม · แนวถนน (CORRIDOR) → ขอบเขตครอบแนวตามความกว้างเดิม (แก้ต่อในฟอร์มได้) ·
  ทั้งตำบล (TAMBON ไม่มีพิกัด) → วงกลมรอบจุดกลางตำบล รัศมี 1.5–6 กม. ตามระยะถึงตำบลข้างเคียง (บอกในข้อความว่าเป็นค่าโดยประมาณ)
  ตัดรหัสรายงานของ arankub (`FR-…` รูปแบบเดียวกับของเรา) ออกจากข้อความ · เติม "ที่มา: arankub.com" ท้ายข้อความ (ปิดได้) · เวลาประกาศใช้เวลาเดิมของต้นทาง
  นำเข้าซ้ำได้ — ตาราง `flood_zone_import` จำรหัสต้นทางไว้ ข้ามรายการที่เคยนำเข้าแล้ว (เก็บข้อมูลต้นฉบับไว้ใน `raw_json`)
  ระบบสร้างตารางเองตอนเปิดหน้านี้ครั้งแรก (ไม่มีสิทธิ์ CREATE → รัน `sql/10_flood_zone_import.sql`) · เปลี่ยนที่อยู่ข้อมูลได้ด้วย `define('ARANKUB_API_URL', '...');` ใน config
  ควรแจ้งเจ้าของ arankub.com ก่อนนำข้อมูลมาเผยแพร่ต่อ

## ฐานข้อมูล

| ตาราง | เก็บอะไร |
|-------|---------|
| `flood_amphoe` / `flood_tambon` | 9 อำเภอ 59 ตำบลของสระแก้ว (พร้อมจุดกึ่งกลางตำบล) |
| `flood_user` / `flood_team` | ผู้ใช้ (รหัสผ่าน bcrypt) / ทีมช่วยเหลือ |
| `flood_zone` | พื้นที่ประกาศ (วงกลม หรือ polygon) |
| `flood_zone_level` | ระดับพื้นที่ (ชื่อ สี ไอคอน ลำดับ) — ผู้ดูแลแก้ได้ในหน้า "ระดับพื้นที่" |
| `flood_zone_import` | รหัสพื้นที่ต้นทางที่นำเข้าแล้ว (arankub.com) + ข้อมูลต้นฉบับ — กันนำเข้าซ้ำ |
| `flood_report` | รายงานจุดน้ำจากประชาชน |
| `flood_help` / `flood_help_log` | ใบงานขอความช่วยเหลือ / ไทม์ไลน์ |
| `flood_vulnerable` / `flood_vulnerable_log` | ทะเบียนกลุ่มเปราะบาง / ประวัติการติดตาม |
| `flood_attachment` | รูปแนบ `ref_type` = report / help / zone (ภาพประกอบพื้นที่) — ไฟล์อยู่ใน `public/uploads/flood/` |
| `flood_login_log` / `flood_audit_log` | ประวัติการเข้าใช้งาน / การเพิ่ม-แก้ไข-ลบ (แนวเดียวกับ COC) |

## ความปลอดภัยที่ทำไว้
- รหัสผ่าน `password_hash` (bcrypt) — ล็อกอินผิด 10 ครั้งใน 15 นาที พักบัญชีชั่วคราว, เปลี่ยน session id หลังล็อกอิน
- ทุก POST ต้องมี CSRF token · คุกกี้ session แยกชื่อ (`FLOODSESSID`) และผูก path `/Flood2026/` ไม่ปนกับ `/coc`
- หน้าสาธารณะแสดงเฉพาะพื้นที่ประกาศ — ชื่อ/เบอร์/ที่อยู่ของประชาชนเห็นเฉพาะเจ้าหน้าที่
- รูปที่อัปโหลด: ตรวจชนิดไฟล์จริง ย่อ ≤1600px บันทึกเป็น JPEG ใหม่ (ข้อมูล EXIF/พิกัดในรูปหลุดทิ้ง) และเปิดได้ผ่านระบบที่ตรวจสิทธิ์เท่านั้น
- ฟอร์มสาธารณะกันสแปม: ช่องดักบอท + จำกัดต่อ IP / ต่อเบอร์โทร (`PUBLIC_LIMIT_*` ใน config)

## CSS (Tailwind)
UI ใช้ Tailwind 3 (ชื่อ class แบบ Bootstrap 3 เหมือน COC) + Bootstrap JS + jQuery — ไฟล์ที่ build แล้วอยู่ใน `public/css/tailwind.css`

```
npm install
npm run build:css     # หลังแก้ src/css/input.css หรือ class ใน views
```

## แผนที่
ใช้ Leaflet 1.9.4 + แผนที่ OpenStreetMap (ตั้งค่าได้ใน `MAP_*` ของ `config/app.php`)
ถ้ามีผู้ใช้จำนวนมาก ควรเปลี่ยน `MAP_TILE_URL` เป็นผู้ให้บริการแผนที่ของหน่วยงาน ตามนโยบายการใช้งานของ OpenStreetMap
